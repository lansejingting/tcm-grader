# ============ 大模型 / RAG ============
openai>=1.0.0          # 官方 SDK（DashScope 兼容模式）
faiss-cpu>=1.7.4       # 向量索引（CPU 版；GPU 用户可换 faiss-gpu）
requests>=2.31.0       # API 直调兜底

# ============ 视觉分类器 ============
# PyTorch（默认从 PyPI 安装 CPU 版；GPU 用户建议安装 CUDA 版，见 README）
torch>=2.0.0
torchvision>=0.15.0

# ============ 通用 ============
numpy>=1.24.0          # 向量运算
Pillow>=10.0.0         # 图片预处理
matplotlib>=3.7.0      # 混淆矩阵可视化
